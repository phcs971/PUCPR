name: robotxp_dashboard
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  charts_flutter: ^0.12.0
  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  flutter:
    sdk: flutter
  flutter_mobx: ^2.0.6+1
  get_it: ^7.2.0
  http: ^0.13.4
  intl: ^0.17.0
  mobx: ^2.0.7+2

dev_dependencies:
  build_runner: ^2.1.11
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.0.7

flutter:
  uses-material-design: true
  assets:
    - assets/robot.png
    - assets/logo.jpeg
